// Operadores de comparacion

const A = 20
const B = 30

// == --> Signo que dice si una variable o un valor es igual a otro. 
    
   let info = A == B

    console.log(info)


// === --> Valida y compara tanto el valor como el tipo de dato. 

    let info2 = A === B
    console.log(info2)


// != !== --> Este signo dice si un valor es diferente a otro.

    console.log(A != B)
    console.log(A !== B)
    



// > --> Este signo dice si un numero es mayor que otro numero.

// < --> Este signo dice si un numero es menos que otro numero.

// >= --> Indica si un numero es mayor o igual a otro numero.

// <= --> Indica si un numero es menos o igual a otro numero.



